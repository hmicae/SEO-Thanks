{% extends 'base.html.twig' %}

{% block title %}Plants{% endblock %}

{% block body %}
    <h1>Plants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ plant.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ plant.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plants_index') }}">back to list</a>

    <a href="{{ path('app_plants_edit', {'id': plant.id}) }}">edit</a>

    {{ include('plants/_delete_form.html.twig') }}
{% endblock %}
