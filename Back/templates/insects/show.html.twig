{% extends 'base.html.twig' %}

{% block title %}Insects{% endblock %}

{% block body %}
    <h1>Insects</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ insect.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ insect.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ insect.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_insects_index') }}">back to list</a>

    <a href="{{ path('app_insects_edit', {'id': insect.id}) }}">edit</a>

    {{ include('insects/_delete_form.html.twig') }}
{% endblock %}
