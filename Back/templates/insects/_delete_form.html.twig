<form method="post" action="{{ path('app_insects_delete', {'id': insect.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ insect.id) }}">
    <button class="btn">Delete</button>
</form>
